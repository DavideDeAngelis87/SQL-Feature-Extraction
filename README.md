# SQL-feature-extraction
Pipeline SQL sviluppata per la trasformazione di dati transazionali bancari relazionali in una tabella denormalizzata (Feature Store) riferita al singolo cliente (id_cliente)
